return {
  "norcalli/nvim-colorizer.lua",
  event = "BufEnter",
  opts = {},
  -- config = function(opts)
  --   local colorizer = require("colorizer")
  --   colorizer.setup(opts)
  --   vim.api.create_autocmd("BufEnter", { callback = function(e) ColorizerAttachToBuffer end })
  -- end,
}
